# Portable entry

Use the project setup command from the jstack checkout to install the runtime and
your host adapter together. Setup replaces this reference with the Codex or Claude
Code adapter. If `.jstack/jstack.py` is absent, the helper is not installed; do not
pretend that receipt or session commands succeeded. See the repository README for
setup. The workflow text remains readable in other hosts, but only the documented
Codex and Claude Code layouts are supported by the installer.
